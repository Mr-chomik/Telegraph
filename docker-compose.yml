services:
  db:
    image: postgres:16
    restart: unless-stopped
    environment:
      POSTGRES_USER: fun
      POSTGRES_PASSWORD: fun
      POSTGRES_DB: fun
    ports:
      - "5432:5432"
    volumes:
      - fun-db-pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U fun -d fun"]
      interval: 5s
      timeout: 5s
      retries: 12

  web:
    build:
      context: .
      dockerfile: apps/web/Dockerfile
    restart: unless-stopped
    depends_on:
      db:
        condition: service_healthy
    env_file: .env
    environment:
      # Inside the compose network the database host is the `db` service.
      DATABASE_URL: postgresql://fun:fun@db:5432/fun
    ports:
      - "3000:3000"
    healthcheck:
      test: ["CMD", "wget", "--spider", "-q", "http://localhost:3000/login"]
      interval: 10s
      timeout: 5s
      retries: 6
      start_period: 20s
    volumes:
      - fun-data:/app/data

  worker:
    build:
      context: .
      dockerfile: apps/worker/Dockerfile
    restart: unless-stopped
    depends_on:
      db:
        condition: service_healthy
    env_file: .env
    environment:
      DATABASE_URL: postgresql://fun:fun@db:5432/fun
    volumes:
      - fun-data:/app/data

  # Optional local LLM. Start with: docker compose --profile ai up -d
  ai:
    image: ollama/ollama:latest
    profiles: ["ai"]
    restart: unless-stopped
    volumes:
      - ollama:/root/.ollama
    ports:
      - "11434:11434"

volumes:
  fun-db-pgdata:
  fun-data:
  ollama: